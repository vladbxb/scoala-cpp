#include <iostream>
#include <fstream>

using namespace std;

int main() {
	ifstream fin("bac.txt", ios::in);

	int nr, prev=1, counter=0, lungime=0, maxim=0;
	bool change = false;
	fin >> nr;
	counter++;

	while (!fin.eof()) {
		if (prev	
					
	}	
	fin.close();
	
	return 0;
}
